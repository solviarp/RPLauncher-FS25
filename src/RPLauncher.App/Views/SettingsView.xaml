<UserControl x:Class="RPLauncher.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBlock Text="Paramètres" Style="{StaticResource TitleText}" Margin="0,0,0,20"/>

        <Border Background="{StaticResource BgCard}" CornerRadius="12" Padding="20" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Installation du jeu" FontWeight="SemiBold"/>
                <TextBlock x:Name="GamePathText" Style="{StaticResource SubtleText}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="Dossier du profil RP" FontWeight="SemiBold" Margin="0,16,0,0"/>
                <TextBlock x:Name="RpProfilePathText" Style="{StaticResource SubtleText}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                <Button Content="Relancer l'assistant de configuration" Style="{StaticResource SecondaryButton}"
                        HorizontalAlignment="Left" Margin="0,14,0,0" Click="RelaunchWizard_Click"/>
            </StackPanel>
        </Border>

        <Border Background="{StaticResource BgCard}" CornerRadius="12" Padding="20">
            <StackPanel>
                <TextBlock Text="URL du manifeste modpack.json" FontWeight="SemiBold"/>
                <TextBox x:Name="ManifestUrlTextBox" Margin="0,8,0,0"/>
                <Button Content="Enregistrer" Style="{StaticResource PrimaryButton}"
                        HorizontalAlignment="Left" Margin="0,14,0,0" Click="Save_Click"/>
                <TextBlock x:Name="SavedText" Foreground="{StaticResource AccentGreen}" Margin="0,10,0,0" Visibility="Collapsed" Text="Enregistré."/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
